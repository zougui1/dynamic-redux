import { createSpecificActionDecorator } from '../../utils/createSpecificActionDecorator';
import { ActionKinds } from '../../common';

export const Map = createSpecificActionDecorator(ActionKinds.MAP);
